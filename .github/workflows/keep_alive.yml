name: Keep Streamlit app alive

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping dashboard
        run: curl -s -o /dev/null https://real-estate-pt.streamlit.app/
